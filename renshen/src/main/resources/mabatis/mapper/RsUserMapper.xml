<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.renshen.pro.renshen.dao.RsUserDao">

    <select id="getUserById" resultType="RsUser" parameterType="Integer">
    select
    id ,
	 username ,
	 sex ,
	 age ,
	 headphotos ,
	 email ,
	 address ,
	 phone ,
	 category ,
	 weixin ,
	 PASSWORD
	from
	rs_user
	where id=#{id}
    </select>
</mapper>
